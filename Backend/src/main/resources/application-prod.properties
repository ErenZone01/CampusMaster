# Server
server.port=${PORT:8080}
spring.web.error.include-message=always
spring.web.error.include-binding-errors=always

# Database (Render PostgreSQL)
spring.datasource.url=jdbc:postgresql://dpg-d7fvuu9j2pic7383bs00-a.oregon-postgres.render.com:5432/campusmaster_db_c3tg?sslmode=require
spring.datasource.username=campusmaster_db_c3tg_user
spring.datasource.password=BIABt6RfFv7Wko5hTDwHsyLFWh12kdyh
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Security - JWT
jwt.secret=${JWT_SECRET:changeMe_in_production}
jwt.expiration=${JWT_EXPIRATION:604800000}

# API Docs (disabled in production for security)
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

# File Upload (using /tmp for ephemeral storage on Render)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
file.upload-dir=${FILE_UPLOAD_DIR:/tmp/uploads}

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
